# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
# Un patrón, no una lista: el despliegue crea .next.new/.old/.prev/.failed y
# cualquier QA visual compila en el suyo (NEXT_BUILD_DIR). Enumerarlos uno a
# uno dejaba el siguiente sin ignorar, y un build de 50 MB acababa en
# `git status` pidiendo que se commiteara.
/.next*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example
!.env.production

# typescript
*.tsbuildinfo
next-env.d.ts

# editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/

# Bundles esbuild para ejecutar en el VPS: artefactos derivados, se
# regeneran desde su .mts (ver handoff). Un bundle viejo en el repo puede
# ejecutar logica de gate obsoleta contra produccion: nunca se versionan.
scripts/*.bundle.mjs
